# Development

To install dependencies:

```bash
bun install
```

To run:

```bash
bun database:dev
bun server:dev
bun mobile:dev
```

To regenerate types after you run a migration do:

```bash
bun codegen
```
